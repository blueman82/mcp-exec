#!/bin/bash
# Pre-push hook: Full lint check before pushing
# Runs ruff, black, isort (~10 seconds)

set -e

SCRIPT_DIR="$(cd "$(dirname "$0")/.." && pwd)"
cd "$SCRIPT_DIR"

echo "Running pre-push validation..."
./infrastructure/validate.sh

echo "Pre-push validation passed!"

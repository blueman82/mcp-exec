#!/bin/bash
# Pre-commit hook: Quick lint check before committing
# Runs ruff only for speed (~5 seconds)

set -e

SCRIPT_DIR="$(cd "$(dirname "$0")/.." && pwd)"
cd "$SCRIPT_DIR"

echo "Running pre-commit checks..."
./infrastructure/validate.sh --quick

echo "Pre-commit checks passed!"

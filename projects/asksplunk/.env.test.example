# AWS Configuration for Integration Tests
# Copy this file to .env.test and update with your AWS profile name
# Each developer can use their own profile without committing credentials

# Example profiles:
# AWS_PROFILE=my-aws-profile
# AWS_PROFILE=adobe-prod
AWS_PROFILE=

# Meta-MCP Server Environment Variables
# Copy this file to .env and fill in your values

# Path to your servers.json configuration
SERVERS_CONFIG=~/.meta-mcp/servers.json

# Note: Backend PATs (Jira, GitHub, etc.) should be configured via:
# - backendAuth in servers.json, OR
# - backendAuthEnvFile pointing to a separate .env file
# See README.md for Gateway integration details

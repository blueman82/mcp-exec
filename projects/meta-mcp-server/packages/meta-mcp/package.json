{
  "name": "@justanothermldude/meta-mcp-server",
  "version": "0.1.12",
  "description": "Meta MCP server that wraps multiple backend MCP servers, exposing only 3 meta-tools to reduce context token consumption",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "meta-mcp-server": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "test": "vitest run"
  },
  "dependencies": {
    "@justanothermldude/meta-mcp-core": "^0.1.0",
    "@modelcontextprotocol/sdk": "^1.24.3",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^20.10.6",
    "typescript": "^5.3.3",
    "vitest": "^4.0.15"
  }
}

{
  "$schema": "../../node_modules/@justanothermldude/mcp-exec-oss-core/servers.schema.json",
  "mcpServers": {
    "filesystem": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "/tmp"],
      "description": "Filesystem MCP server for integration testing"
    }
  }
}
